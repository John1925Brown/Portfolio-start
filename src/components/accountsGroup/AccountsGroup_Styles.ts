import styled from 'styled-components';

const AccountsGroup = styled.div`
  display: flex;
  align-items: center;
  justify-content: space-around;
  flex-wrap: wrap;
  gap: 50px;
`;

export const S = {
  AccountsGroup,
};
