import styled from 'styled-components';

const StyledFooter = styled.footer`
  padding-top: 42px;
  padding-bottom: 42px;
`;

const ContactsText = styled.p`
  font-weight: 600;
  font-size: 22px;
`;

export const S = {
  ContactsText,
  StyledFooter,
};
